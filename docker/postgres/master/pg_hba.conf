# PostgreSQL Client Authentication Configuration File for Master

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
host    all             all             172.21.0.0/16           md5

# IPv6 local connections:
host    all             all             ::1/128                 trust

# Allow replication connections from replica
host    replication     postgres        172.21.0.3/32           md5
host    replication     repl_user       172.21.0.3/32           md5